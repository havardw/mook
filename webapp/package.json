{
    "name": "mook-webapp",
    "version": "0.1.1",
    "description": "Web client for Mook",
    "devDependencies": {
        "http-server": "^0.6.1"
    },
    "scripts": {
        "prestart": "npm install",
        "start": "http-server files -a localhost -p 8000 -c-1"
    }
}
